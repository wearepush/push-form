language: elixir

elixir:
  - '1.13.1'

otp_release:
  - '24.1'

addons:
  postgresql: '14'

services:
  - postgresql

env:
  - MIX_ENV=test
  - SECRET_KEY_BASE=test

before_script:
  - cp config/travis.exs config/test.exs
  - mix do ecto.create, ecto.migrate

script:
  - mix test
